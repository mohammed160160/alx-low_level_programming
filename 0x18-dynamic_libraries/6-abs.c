#include "main.h"

/**
 * _abs - gives out the absolute value of the given number
 * @n: the value the checker do
 * Return: The absolute value of n
 */

int _abs(int n)
{

	if (n < 0)
	{
	return (-n);
	}
	else
	{
	return (n);
	}

}
