#include "main.h"
#include <stdio.h>

/**
 * _strlen - determain the length of a string
 * @s:The pointer to the string
 * Return: the string length of s
 */
int _strlen(char *s)
{
char x = *s;
int y

y = strlen(x);
return(y);
}
