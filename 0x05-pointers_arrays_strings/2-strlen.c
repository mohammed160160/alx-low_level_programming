#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - determain the length of a string
 * @s:The pointer to the string
 * Return: Null
 */
int _strlen(char *s)
{
strlen(*s);
return;
}
