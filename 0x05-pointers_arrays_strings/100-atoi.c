#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * _atoi - prints the value of integer in a string
 * @s: The string the function is working on
 * Return: an integer
 */

int _atoi(char *s)
{
int n;

n = atoi(s);
return (n);
}
