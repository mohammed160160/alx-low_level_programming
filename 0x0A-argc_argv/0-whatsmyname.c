#include <stdio.h>

/**
 * main - prints out the name of the function used
 * @argc: The argument counter
 * @argv: The argument vector(array)
 * Return: Success
 */

int main(int argc, char *argv[])
{
(void) argc;
printf("%s\n", argv[0]);
return (0);
}
