#include "main.h"

/**
 * _abs - checks if the char is positive, negative or zero
 * @n: the value the checker do
 * Return: 1 if positive, -1 if negative , 0 if zero
 */

int _abs(int n)
{
	if (n < 0)
	{
	return (-n);
	}
	else
	{
	return (n);
	}
	}
